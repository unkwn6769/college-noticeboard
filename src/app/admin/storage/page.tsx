"use client";
import { useEffect,useState } from "react";
export default function StoragePage(){const [data,setData]=useState<any>(null);useEffect(()=>{fetch("/api/admin/storage").then(r=>r.json()).then(setData);},[]);if(!data)return <p>Loading…</p>;const s=data.stats;return <><h1>Storage health</h1><div className="stats"><div className="card"><div className="meta">Usage</div><div className="stat-value">{s.usagePercent.toFixed(1)}%</div><div>{s.pressure}</div></div><div className="card"><div className="meta">Free</div><div className="stat-value">{(s.freeBytes/1024/1024/1024).toFixed(1)} GB</div></div><div className="card"><div className="meta">PostgreSQL</div><div className="stat-value">{(Number(data.postgresBytes)/1024/1024).toFixed(1)} MB</div></div><div className="card"><div className="meta">Pending cleanup</div><div className="stat-value">{data.pendingCleanup}</div></div></div></>}
