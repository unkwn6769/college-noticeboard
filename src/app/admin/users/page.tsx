"use client";
import { useEffect, useState, type FormEvent } from "react";
export default function UsersPage(){const [users,setUsers]=useState<any[]>([]);const [msg,setMsg]=useState("");async function load(){const r=await fetch("/api/admin/users");const d=await r.json();setUsers(d.users??[]);}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password"),displayName:f.get("displayName"),role:f.get("role")})});const d=await r.json();setMsg(r.ok?"User created":(d.error??"Create failed"));if(r.ok){e.currentTarget.reset();await load();}}async function disable(id:string){const r=await fetch(`/api/admin/users/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"DISABLED"})});const d=await r.json();setMsg(r.ok?"User disabled":(d.error??"Update failed"));await load();}return <><h1>Users</h1><div className="card" style={{marginBottom:20}}><h2>Create user</h2>{msg&&<p className="muted">{msg}</p>}<form className="form" onSubmit={submit}><input name="displayName" placeholder="Display name" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" minLength={10} placeholder="Password" required/><select name="role" defaultValue="USER"><option value="USER">USER</option><option value="ADMIN">ADMIN</option><option value="OWNER">OWNER</option></select><button className="btn">Create</button></form></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.displayName}</td><td>{u.email}</td><td>{u.role}</td><td>{u.status}</td><td>{u.status==="ACTIVE"&&<button className="btn danger" onClick={()=>disable(u.id)}>Disable</button>}</td></tr>)}</tbody></table></div></>}
